import styled, { css } from 'styled-components';

const baseStyles = css``;

const H1 = styled.h1`
  ${baseStyles}
`;

const H2 = styled.h2`
  ${baseStyles}
`;

const Heading = {
  H1,
  H2,
};

export default { Heading };

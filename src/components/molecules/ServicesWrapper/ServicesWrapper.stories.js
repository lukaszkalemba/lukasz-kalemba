import React from 'react';
import ServicesWrapper from './ServicesWrapper';

export const servicesWrapper = () => (
  <ServicesWrapper animation={{}} images={{}} />
);

export default {
  component: ServicesWrapper,
  title: 'Molecules|ServicesWrapper',
};

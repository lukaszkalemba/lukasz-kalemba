const wrapper = {
  variants: {
    animate: {
      transition: {
        staggerChildren: 0.75,
        delayChildren: 0.25,
      },
    },
  },
  initial: 'initial',
  animate: 'animate',
};

export default {
  wrapper,
};

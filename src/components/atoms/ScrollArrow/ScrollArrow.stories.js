import React from 'react';
import ScrollArrow from './ScrollArrow';

const handleArrowClick = () => {};

export const scrollArrow = () => <ScrollArrow onClick={handleArrowClick} />;

export default {
  component: ScrollArrow,
  title: 'Atoms|ScrollArrow',
};
